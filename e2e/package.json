{
  "name": "majstr-e2e",
  "private": true,
  "description": "Playwright smoke tests against the Vite SPA with a fully stubbed API",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
